# Chris Fork 变更历史

本文件记录 `chris-changes` 相对 upstream Vibe-Research 的功能、修复和验证历史；它不承担 Fork 使用说明。当前功能概览见 [README_FORK.md](README_FORK.md)。

## 2026-08-12 — 后端配置与界面调整

### 后端可编辑配置

- 新增 `backend/backend_config.json`，集中维护 A 股指数、全球指数和研报文件名分类关键词。
- 后端启动时读取并校验配置；JSON 无效、必要字段缺失或关键词为空时会给出明确错误并阻止启动。
- A 股指数按 `tencent`、全球指数按 `eastmoney` 数据源路由；研报分类按配置数组顺序确定优先级。
- 修改配置后需重启后端。

### UI / UX

- 板块中心页面标记为“建设中”；侧栏子板块快捷入口默认展开，并可通过独立箭头折叠而不触发导航。
- `sectors.json` 的指数列表明确为未来参考，当前每日复盘指数仍由后端实时获取。
- 持仓页面增加行情加载提示；持仓、购买与清仓表支持按名称或代码筛选、规定排序、表内滚动和固定表头；交易录入与历史默认折叠。
- 持仓最多显示 20 行，购买与清仓历史最多显示 10 行后在各自表内滚动。
- 金额与单价内部保存四位小数，页面统一显示两位；收益显示不包含分红、配股等公司行为。

### 验证

- 前端 `npm.cmd test`：16 项通过。
- 前端 `npm.cmd run build`：通过。
- 配置加载和数据源路由已做离线检查；`backend/.venv` 未安装 `pytest`，新增 pytest 用例未在该环境实际执行。

## 2026-08-10 — 持仓交易账本

- 持仓改由购买交易和清仓交易驱动；购买会增加当前持仓，清仓按当时成本均价扣减，并保存总成本和已实现盈亏快照。
- 购买历史保存名称、日期、成交单价、股数、成本总额和币种；清仓历史保存成交总额、总成本、已实现盈亏和盈亏比例。
- 清仓数量不能超过当前持仓；完全清仓后当前持仓移除，交易历史保留。
- 旧版聚合持仓自动迁移为“历史导入”购买记录，不伪造未知买入日期；不再提供直接删除持仓或交易记录的入口，以维持账本一致性。

## 2026-08-09 — 海外证券、缓存与资讯雷达

### 海外证券格式与数据

- 个股数据支持 A 股 `300760`、美股 `AAPL.US`、港股 `00700.HK`、韩股 `005930.KR`；港股短代码自动补零。
- 裸代码和旧 `.KS` 后缀不再受支持，格式错误返回 HTTP 400 及可用示例。
- `gstock.py` 改为依国家后缀构造东财请求，避免依赖失效的证券搜索接口；美股在受限的美国市场编号中探测，港股和韩股使用确定映射。
- 个股页仅对 `.HK` 请求港股现金流；韩股仍只提供行情，不提供东财 F10 指标。

### 只读股票数据缓存

- 后端引入进程内 TTL/LRU 缓存：容量上限 64，命中后更新最近使用顺序，异常结果不缓存，重启后清空。
- 缓存覆盖 A 股数据、估值、研报、新闻、海外综合行情和港股现金流；缓存 key 包含研报页数、新闻条数等结果相关参数。

### 自选与持仓的跨市场支持

- 自选股及每日复盘关注证券支持上述四种市场格式；A 股批量走腾讯行情，海外证券逐只使用东财海外行情。
- 实时轮询仍按 A 股交易时段运行；海外证券在首次加载或手动刷新时更新，并受 60 秒海外行情缓存约束。
- 资讯页的公告与个股新闻聚合会自动跳过海外自选，以避免调用仅支持 A 股的接口。
- 持仓支持上述市场，并按 CNY、USD、HKD、KRW 分别汇总市值、成本、浮动与已实现盈亏，不进行汇率换算。

### 资讯雷达

- 每个赛道可通过一次 AI 调用生成中文要点和非中文标题的简体中文译文；含中文的标题不翻译。
- AI 结果保存在 `backend/.cache/radar.json` 的赛道 `digest` 和新闻项 `zh` 字段，重新进入页面可复用缓存。
- “存入沉淀”仍只保存当前要点到研究记录，其原有行为未改变。
- 刷新创建新的 `snapshot_id` 并原子替换缓存；旧快照的 AI 写入会被 HTTP 409 拒绝。
- RSS 聚合按时间排序后，以忽略大小写和多余空白的规范化标题去重，保留第一条来源。

### 验证

- 前端 `npm.cmd test`：16 项通过；`npm.cmd run build`：通过。
- 后端完成 `py_compile`、离线符号路由/API 格式、TTL/LRU、雷达缓存/旧快照/标题去重检查。
- 只读东财验证取得 `AAPL.US`、`00700.HK`、`005930.KR` 行情及 `00700.HK` 现金流。
- `backend/.venv` 未安装 `pytest`，新增 pytest 用例未在该环境实际执行。
